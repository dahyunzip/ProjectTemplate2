<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.MemberMapper">
	<insert id="insertMember" parameterType="MemberVO">
		INSERT INTO users(username, password, enabled, nickname, address, gender, birthdate)
		VALUES(#{username}, #{password}, true, #{nickname}, #{address}, #{gender}, #{birthdate})
	</insert>
	
	<insert id="insertAuthority" parameterType="map">
		INSERT INTO authorities(username, authority)
		VALUES(#{username}, #{authority})
	</insert>
	
	<select id="selectByUsername" parameterType="String" resultType="MemberVO">
		SELECT username, password, enabled, nickname, address, gender, birthdate
		FROM users
		WHERE username = #{username}
	</select>
	
	<update id="updateMemberInfo">
		UPDATE users
		SET
			nickname = #{nickname},
			address = #{address}
		WHERE username = #{username}
	</update>
	
	<update id="updateEnabled">
		UPDATE users
		SET enabled = #{enabled}
		WHERE username = #{username}
	</update>
</mapper>